// Cache the DOM


const addTaskBtn = document.querySelector("#add-a-task");
const addTaskModal = document.querySelector("#add-a-task-modal");
const closeModalButton = document.querySelectorsAll(".close-modal-button");
const submitAddTaskBtn = document.querySelector("#add-a-task-submit");
const addProjectBtn = document.querySelector("#add-a-project");
const submitAddProjectBtn = document.querySelector("#add-a-project-submit");

// Attach Event Listeners
addTaskBtn.addEventListener("click", .addTaskThruModal);
addBookBtn.addEventListener("click", myLibrary.toggleModal);
closeModalButton.addEventListener("click", myLibrary.toggleModal);

// Attach event listeners


